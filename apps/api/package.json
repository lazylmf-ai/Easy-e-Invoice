{
  "name": "@einvoice/api",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for Easy e-Invoice",
  "main": "./src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "@hono/zod-validator": "^0.2.0",
    "drizzle-orm": "^0.29.0",
    "jose": "^5.2.0",
    "zod": "^3.22.0",
    "resend": "^3.2.0",
    "@einvoice/database": "workspace:*",
    "@einvoice/validation": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240423.0",
    "wrangler": "^3.48.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  }
}